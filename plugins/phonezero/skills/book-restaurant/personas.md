# Book-restaurant personas

Restaurant host scripts live in [`docs/PERSONAS.md`](../../../../docs/PERSONAS.md). After any change to `prompts/voice-agent.md` or this skill’s classify gates, re-run those scenarios. Outcome `booked` is an alias of runtime `succeeded`.
